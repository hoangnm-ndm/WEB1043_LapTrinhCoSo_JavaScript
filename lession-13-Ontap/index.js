/**
 * 1. Tạo một form đăng ký tài khoản mới bao gồm username, email, password.
 * 2. Khi blur khỏi ô password, kiểm tra xem password cần phải có ít nhất 8 ký tự, nếu không có: in ra màn hình cảnh báo.
 * 3. Khi ấn nút Send -> console.log() ra một object như bên dưới
 */

const user = {
  username: "username do nguoi dung nhap vao",
  email: "email do nguoi dung nhap vao",
  password: "password do nguoi dung nhap vao",
};
